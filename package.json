{
  "name": "multiscan",
  "private": true,
  "workspaces": ["raycast", "worker", "website"],
  "scripts": {
    "dev:worker": "npm run dev --workspace=worker",
    "dev:website": "npm run dev --workspace=website",
    "dev:raycast": "npm run dev --workspace=raycast",
    "build:website": "npm run build --workspace=website",
    "deploy": "npm run build --workspace=website && rm -rf worker/dist && cp -r website/dist worker/dist && npm run deploy --workspace=worker"
  }
}
