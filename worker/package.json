{
  "name": "multiscan-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "update-cosmos": "npx tsx scripts/update-cosmos-chains.ts"
  },
  "dependencies": {
    "@noble/hashes": "^1.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.8.0",
    "wrangler": "^3.0.0"
  }
}
